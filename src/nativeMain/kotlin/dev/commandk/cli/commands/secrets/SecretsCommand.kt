package dev.commandk.cli.commands.secrets

import com.github.ajalt.clikt.core.CliktCommand
class SecretsCommand : CliktCommand("Perform operations on secrets via the CommandK CLI", name = "secrets") {
    override fun run() {}
}
